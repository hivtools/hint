describe("Forgot password component", () => {
    //renders form with no error

    //renders with error

    //renders with link requested message

    //invokes requestLink action

    //does not requestLink action if form is invalid - empty, not email address
});